import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "x402 Arbitrage Mesh — Sovereign Agent Gateway",
  description: "Decentralized AI Agent Load Balancer and Payment Router on Stellar Soroban. WASM-sandboxed, L402-secured, zero-trust.",
  openGraph: {
    title: "x402 Arbitrage Mesh",
    description: "The world's first Agent-to-Agent Payment Router with WASM Quarantine, built on Stellar.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
